# OpenBridge — Task List

> **Pending:** 10 | **In Progress:** 0 | **Done:** 11
> **Last Updated:** 2026-02-27

<details>
<summary>Archive (476 tasks completed across Phases 1–62)</summary>

- [V0 — Phases 1–5](archive/v0/TASKS-v0.md)
- [V1 — Phases 6–10](archive/v1/TASKS-v1.md)
- [V2 — Phases 11–14](archive/v2/TASKS-v2.md)
- [MVP — Phase 15](archive/v3/TASKS-v3-mvp.md)
- [Self-Governing — Phases 16–21](archive/v4/TASKS-v4-self-governing.md)
- [E2E + Channels — Phases 22–24](archive/v5/TASKS-v5-e2e-channels.md)
- [Smart Orchestration — Phases 25–28](archive/v6/TASKS-v6-smart-orchestration.md)
- [AI Classification — Phase 29](archive/v7/TASKS-v7-ai-classification.md)
- [Production Readiness — Phase 30](archive/v8/TASKS-v8-production-readiness.md)
- [Memory + Scale — Phases 31–38](archive/v9/TASKS-v9-memory-scale.md)
- [Memory Wiring — Phase 40](archive/v10/TASKS-v10-memory-wiring.md)
- [Memory Fixes — Phases 41–44](archive/v11/TASKS-v11-memory-fixes.md)
- [Post-v0.0.2 — Phases 45–50](archive/v12/TASKS-v12-post-v002-phases-45-50.md)
- [v0.0.3 — Phases 51–56](archive/v13/TASKS-v13-v003-phases-51-56.md)
- [v0.0.4 — Phases 57–62](archive/v14/TASKS-v14-v004-phases-57-62.md)

</details>

---

## Phase 63 — Fix FTS5 Syntax Error (OB-F38)

> **Priority:** Medium — FTS5 special characters crash cross-session context injection silently.
> **Finding:** [OB-F38](FINDINGS.md#2--ob-f38--fts5-syntax-error-on-special-characters--cross-session-context-silently-fails)
> **Effort:** Tiny (~10 LOC + tests). Unblocks Phase 64 (conversation context must work before memory.md injection).

| #   | Task ID | Description                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Status                                                                            |
| --- | ------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------- | ------- |
| 1   | OB-1113 | **Extract shared FTS5 sanitizer and apply to `searchConversations()`** — A `sanitizeFts5Query()` function already exists in `src/memory/conversation-store.ts` (lines 40–47). It strips FTS5 special characters (`"*(){}[]:^~?@#$%&\|<>=!+,;`) and quotes each token. **Step 1:** Move this function to `src/memory/retrieval.ts` and export it (or create `src/memory/fts5-utils.ts`). **Step 2:** In `searchConversations()` at `src/memory/retrieval.ts` line 252, call the sanitizer on `query` before passing to the FTS5 `MATCH` clause: `const sanitized = sanitizeFts5Query(query); if (!sanitized) return [];`. **Step 3:** Update `src/memory/conversation-store.ts` to import from the shared location instead of using its local copy. Remove the local duplicate. | ✅ Done                                                                           |
| 2   | OB-1114 | **Unit tests for FTS5 escaping in `searchConversations()`** — Add tests to `tests/memory/retrieval.test.ts` (create file if needed). Test cases: (a) query with single quotes `it's` — no `SqliteError`, returns results or empty array; (b) query with double quotes `"hello"` — same; (c) query with parentheses `(test)` — same; (d) query with asterisks `test*` — same; (e) query with FTS5 operators `AND OR NOT` as literal words — same; (f) empty string returns `[]`; (g) string of only special characters returns `[]`. Use an in-memory SQLite database with the `conversations` + `conversations_fts` tables for testing (follow the pattern in existing `tests/memory/` test files).                                                                            | ✅ Done                                                                           |
| 3   | OB-1115 | **Final validation for OB-F38** — Run `npm run test`, `npm run typecheck`, `npm run lint`, `npm run build`. Confirm 0 errors. In `docs/audit/FINDINGS.md`: change OB-F38 severity from `🟡 Medium` to `✅ Fixed`, update the Priority Order table status to `Fixed`, update the header counters to `Open: 2                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Fixed: 38`. In `docs/audit/TASKS.md`: mark this task ✅ Done and update counters. | ✅ Done |

---

## Phase 64 — Fix memory.md Updates (OB-F39)

> **Priority:** HIGH — memory.md never gets meaningful updates. Master runs stateless, has no conversation context.
> **Finding:** [OB-F39](FINDINGS.md#1--ob-f39--memorymd-never-updates--master-runs-in---print-mode-stateless)
> **Depends on:** Phase 63 (FTS5 fix must land first — conversation retrieval must work correctly).
> **Approach:** Option C from finding — inject recent conversation history from SQLite into the memory-update prompt. Keeps `--print` mode architecture unchanged.

| #   | Task ID | Description                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Status                                                                            |
| --- | ------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------- | ------- |
| 4   | OB-1116 | **Add `getRecentMessages()` to conversation store** — In `src/memory/conversation-store.ts`, add a new exported function: `export function getRecentMessages(db: Database.Database, limit = 20): ConversationEntry[]`. It should query `SELECT id, session_id, role, content, channel, user_id, created_at FROM conversations WHERE role IN ('user', 'master') ORDER BY created_at DESC LIMIT ?` with the `limit` parameter. Return results in chronological order (oldest-to-newest, so `.reverse()` after fetch). Then expose it on the `MemoryManager` facade in `src/memory/index.ts`: `async getRecentMessages(limit?: number): Promise<ConversationEntry[]>` — delegates to the store function. This provides `triggerMemoryUpdate()` with the recent conversation history it needs. | ✅ Done                                                                           |
| 5   | OB-1117 | **Inject conversation history into memory-update prompt** — In `src/master/master-manager.ts`, update `triggerMemoryUpdate()` (lines 865–888). Before building the prompt, call `const recentMessages = await this.memory?.getRecentMessages(20) ?? []`. Format each entry as `[YYYY-MM-DD HH:MM] Role: content` (truncate content to 300 chars with `…`). If messages exist, prepend a `## Recent conversation history:\n` section to the prompt string so the stateless `--print` agent has actual conversation context to write meaningful memory notes. If no messages exist, send the prompt as-is (graceful degradation).                                                                                                                                                            | ✅ Done                                                                           |
| 6   | OB-1118 | **Make memory-update prompt tool-agnostic** — In `src/master/master-manager.ts` `triggerMemoryUpdate()`, the current prompt says `Use the Write tool to write your updated notes to ${memoryPath}`. `Write` is a Claude Code tool name — Codex uses different tool naming. Replace with generic language: `Write your updated notes to ${memoryPath}.` (remove the explicit tool name reference). The AI will use whatever write capability it has. This ensures the prompt works correctly when Codex is the Master AI.                                                                                                                                                                                                                                                                   | ✅ Done                                                                           |
| 7   | OB-1119 | **Improve memory update error logging** — In `src/master/master-manager.ts` `triggerMemoryUpdate()`: (a) Add `logger.info({ messageCount: recentMessages.length }, 'Starting memory update')` at the top of the method (after the early return guard). (b) The existing error handling already has `logger.warn({ err }, 'Memory update prompt failed')` in the catch block and `logger.warn({ exitCode }, 'Memory update prompt returned non-zero exit')` for non-zero exit codes — verify these are present and include useful details. (c) If there's a silent `.catch()` that swallows errors anywhere in the call chain, replace it with a `logger.warn()` that includes the error. The goal: operators can see in logs when memory updates fail and why.                             | ✅ Done                                                                           |
| 8   | OB-1120 | **Unit tests for memory update context injection** — Create `tests/master/memory-update.test.ts`. Test cases: (a) `triggerMemoryUpdate()` spawns an agent whose prompt contains `## Recent conversation history:` when messages exist in SQLite; (b) prompt includes formatted `[timestamp] User:` / `[timestamp] Master:` lines; (c) content is truncated to 300 chars with `…` for long messages; (d) when no messages exist, prompt is sent without the history section (no crash); (e) when `this.memory` is null, prompt is sent without history (graceful fallback). Mock `AgentRunner.spawn()` to capture the prompt. Mock `MemoryManager.getRecentMessages()` to return test data.                                                                                                 | ✅ Done                                                                           |
| 9   | OB-1121 | **Integration test for memory update flow** — In `tests/master/memory-update.test.ts`, add an integration test: (a) create an in-memory SQLite database with the conversations schema, (b) insert 5 test conversation entries (mix of `user` and `master` roles, some with special characters), (c) create a `MemoryManager` with the test database, (d) call `getRecentMessages(20)` and verify it returns the entries in chronological order filtered to `user`/`master` roles, (e) verify the formatted prompt string contains snippets from the test conversations. This validates the full pipeline from SQLite to MemoryManager to formatted prompt.                                                                                                                                 | ✅ Done                                                                           |
| 10  | OB-1122 | **Final validation for OB-F39** — Run `npm run test`, `npm run typecheck`, `npm run lint`, `npm run build`. Confirm 0 errors. In `docs/audit/FINDINGS.md`: change OB-F39 severity from `🟠 High` to `✅ Fixed`, update the Priority Order table status to `Fixed`, update the header counters to `Open: 1                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Fixed: 39`. In `docs/audit/TASKS.md`: mark this task ✅ Done and update counters. | ✅ Done |

---

## Phase 65 — Graceful Shutdown (OB-F40)

> **Priority:** Medium — Ctrl+C force-kills before session state and memory are saved.
> **Finding:** [OB-F40](FINDINGS.md#3--ob-f40--ungraceful-shutdown--ctrlc-force-kills-before-memory-update-completes)
> **Depends on:** Phase 64 (memory update must be context-aware first, so shutdown improvements preserve meaningful data).

| #   | Task ID | Description                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Status                                                                            |
| --- | ------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------- | --------- |
| 11  | OB-1123 | **Add user-facing shutdown message** — In `src/index.ts`, update the `shutdown()` function (lines 337–349). Immediately after setting `shutdownInProgress = true`, add `console.log('\nShutting down gracefully... please wait');` BEFORE `bridge.stop()`. This tells users that shutdown is in progress and they should not press Ctrl+C again. Keep the existing `logger.info('Shutting down...')` line for structured logging.                                                                                                                                                                                                                                                                                                                                                                                              | ✅ Done                                                                           |
| 12  | OB-1124 | **Add shutdown timeout + critical-first ordering** — In `src/index.ts`, wrap the `await bridge.stop()` call inside `Promise.race([bridge.stop(), new Promise((_, reject) => setTimeout(() => reject(new Error('Shutdown timeout')), 10_000))])`. If the timeout fires, log `console.error('Shutdown timeout exceeded (10s) — forcing exit')` and call `process.exit(1)`. **Also** in `src/master/master-manager.ts` `shutdown()` method: reorder operations so `saveMasterSessionToStore()` (fast, less than 100ms SQLite write) runs BEFORE `triggerMemoryUpdate()` (slow, 10-30s AI spawn). This ensures the critical session state is always persisted, even if the memory update is interrupted by the timeout. Add a try/catch around `triggerMemoryUpdate()` so its failure does not block `saveMasterSessionToStore()`. | ◻ Pending                                                                         |
| 13  | OB-1125 | **Unit tests for graceful shutdown** — Create `tests/core/shutdown.test.ts`. Test cases: (a) `shutdownInProgress` guard prevents double-shutdown (second call is a no-op); (b) shutdown timeout fires after configured duration and calls `process.exit(1)`; (c) `console.log` is called with shutdown message on SIGINT; (d) in `MasterManager.shutdown()`, verify `saveMasterSessionToStore()` is called before `triggerMemoryUpdate()` (use mock spies to check call order). Mock `bridge.stop()`, `process.exit()`, `console.log()`.                                                                                                                                                                                                                                                                                       | ◻ Pending                                                                         |
| 14  | OB-1126 | **Final validation for OB-F40** — Run `npm run test`, `npm run typecheck`, `npm run lint`, `npm run build`. Confirm 0 errors. In `docs/audit/FINDINGS.md`: change OB-F40 severity from `🟡 Medium` to `✅ Fixed`, update the Priority Order table status to `Fixed`, update the header counters to `Open: 0                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Fixed: 40`. In `docs/audit/TASKS.md`: mark this task ✅ Done and update counters. | ◻ Pending |

---

## Phase 66 — Documentation + Validation (OB-F38, OB-F39, OB-F40)

> **Priority:** Required — all project docs must reflect the bug fixes from Phases 63–65.
> **Depends on:** Phases 63 + 64 + 65 (all code changes must land first).
> **Pattern:** Matches Phases 59 and 62 (documentation + validation after implementation).

| #   | Task ID | Description                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | Status                                                   |
| --- | ------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------- | --------- |
| 15  | OB-1127 | **Update `docs/API_REFERENCE.md`** — (a) Fix the **incorrect** claim at line 919 that `searchConversations()` already sanitizes FTS5 queries — update to document that `sanitizeFts5Query()` is now applied before MATCH (from Phase 63 fix). (b) Add `getRecentMessages(db, limit): ConversationEntry[]` to the conversation-store API section. (c) Add `MemoryManager.getRecentMessages(limit?)` to the MemoryManager facade section. (d) Document `triggerMemoryUpdate()` behavior: called every 10 completed tasks and on shutdown, injects recent conversation history into the prompt, uses `--print` mode. (e) Expand shutdown documentation: document `shutdownInProgress` guard, 10s timeout with `process.exit(1)` fallback, critical-first ordering (session state saved before memory update).                    | ◻ Pending                                                |
| 16  | OB-1128 | **Update `docs/ARCHITECTURE.md`** — (a) Add a "Graceful Shutdown" section documenting the shutdown flow: `SIGINT -> shutdown() -> console message -> bridge.stop() -> MasterManager.shutdown() -> saveMasterSessionToStore() -> triggerMemoryUpdate() -> process.exit(0)`, with 10s timeout and force-exit fallback. (b) In the Memory System section, expand the memory.md update mechanism: triggered every 10 tasks + on shutdown, injects last 20 conversation entries into the prompt, stateless `--print` mode by design. (c) In the FTS5 search section, document that all FTS5 MATCH queries are sanitized via `sanitizeFts5Query()` to prevent syntax errors from special characters.                                                                                                                                | ◻ Pending                                                |
| 17  | OB-1129 | **Update `docs/TROUBLESHOOTING.md`** — Add three new sections: (a) **"FTS5 / Database Errors"** — symptom: `SqliteError: fts5: syntax error near "'"`, cause: special characters in search queries, resolution: fixed in v0.0.5 via `sanitizeFts5Query()`. (b) **"Memory Not Updating"** — symptom: `memory.md` unchanged across sessions, cause: Master ran stateless without conversation context, resolution: fixed in v0.0.5 with recent conversation history injected into the memory-update prompt. (c) **"Shutdown / Ctrl+C Issues"** — symptom: `[tsx] Previous process hasn't exited yet. Force killing...`, cause: `tsx` force-kills before graceful shutdown completes, resolution: fixed in v0.0.5 with 10s shutdown timeout, session state saved first. Workaround: wait for "Shutting down gracefully" message. | ◻ Pending                                                |
| 18  | OB-1130 | **Update `CONTRIBUTING.md`** — Add a "Development Tips" subsection under the development setup section. Document: (a) Ctrl+C behavior — when running `npm run dev`, press Ctrl+C once and **wait** for "Shutting down gracefully... please wait"; pressing Ctrl+C multiple times triggers `tsx` force-kill. (b) Graceful shutdown takes up to 10 seconds (session state + memory update). (c) If force-killed, session state is still saved (critical-first ordering), but memory.md update may be lost. This fulfills OB-F40 recommended fix #4: "Document tsx force-kill behavior in README/contributing guide".                                                                                                                                                                                                            | ◻ Pending                                                |
| 19  | OB-1131 | **Update `CHANGELOG.md`** — Add a new section under `[Unreleased]` for v0.0.5 bug fixes (Phases 63–66): (a) `fix(memory): sanitize FTS5 queries in searchConversations() — prevents SqliteError on special characters (OB-F38)`. (b) `fix(master): inject recent conversation history into memory-update prompt — memory.md now receives meaningful context (OB-F39)`. (c) `fix(master): make memory-update prompt tool-agnostic — works with Claude and Codex`. (d) `fix(core): graceful shutdown with 10s timeout — session state saved first, then memory update attempted (OB-F40)`. (e) `fix(core): user-facing shutdown message on Ctrl+C`. (f) Documentation updates across API_REFERENCE, ARCHITECTURE, TROUBLESHOOTING, CONTRIBUTING, ROADMAP.                                                                       | ◻ Pending                                                |
| 20  | OB-1132 | **Update `docs/ROADMAP.md` + `CLAUDE.md` (both files)** — (a) In `docs/ROADMAP.md`: add Phases 63–66 as shipped under v0.0.5 milestone — FTS5 sanitization, memory.md context injection, graceful shutdown, documentation. Mark all three findings as resolved. (b) In workspace root `CLAUDE.md`: add v0.0.5 to the "Current state" bullet with summary of fixes, update "Current Development Focus" section. (c) In `OpenBridge/CLAUDE.md`: same updates — v0.0.5 milestone, current state reflects all findings resolved, update memory system description to note FTS5 sanitization and context-aware memory updates.                                                                                                                                                                                                     | ◻ Pending                                                |
| 21  | OB-1133 | **Final v0.0.5 validation** — Run `npm run test`, `npm run typecheck`, `npm run lint`, `npm run build`. Confirm 0 errors. Verify all 3 findings (OB-F38, OB-F39, OB-F40) are marked ✅ Fixed in `docs/audit/FINDINGS.md` with `Open: 0                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | Fixed: 40`. Verify `docs/audit/TASKS.md`shows`Pending: 0 | Done: 21`. Verify `CHANGELOG.md`has v0.0.5 entries. Verify`docs/ROADMAP.md` has Phases 63–66 as shipped. All documentation consistent. | ◻ Pending |

---

## Phase Summary

| Phase  | Name                       | Tasks   | Finding        | Priority | Effort       |
| ------ | -------------------------- | ------- | -------------- | -------- | ------------ |
| **63** | Fix FTS5 Syntax Error      | 3 (3✅) | OB-F38         | Medium   | Tiny         |
| **64** | Fix memory.md Updates      | 7 (7✅) | OB-F39         | HIGH     | Medium       |
| **65** | Graceful Shutdown          | 4 (1✅) | OB-F40         | Medium   | Small        |
| **66** | Documentation + Validation | 7 (0✅) | All 3 findings | Required | Small–Medium |
|        | **Total**                  | **21**  |                |          |              |

---

## Dependency Graph

```
Phase 63 (FTS5 Fix — Medium, runs first) ──────────────────────────────────
  ├── OB-1113 (extract shared sanitizer + apply to searchConversations)
  ├── OB-1114 (unit tests for FTS5 escaping)
  └── OB-1115 (validation + close OB-F38)

Phase 64 (memory.md Fix — HIGH) — depends on Phase 63
  ├── OB-1116 (getRecentMessages helper) ─────────┐
  ├── OB-1117 (inject history into prompt)  ───────┤
  ├── OB-1118 (tool-agnostic prompt)               ├── OB-1120 (unit tests)
  ├── OB-1119 (error logging)              ────────┘       │
  ├── OB-1121 (integration test) ─────────────────────────┘
  └── OB-1122 (validation + close OB-F39)

Phase 65 (Graceful Shutdown — Medium) — depends on Phase 64
  ├── OB-1123 (user-facing shutdown message)
  ├── OB-1124 (timeout + critical-first ordering)
  ├── OB-1125 (unit tests)
  └── OB-1126 (validation + close OB-F40)

Phase 66 (Documentation + Validation) — depends on Phases 63 + 64 + 65
  ├── OB-1127 (API_REFERENCE.md — fix incorrect FTS5 claim + new methods)
  ├── OB-1128 (ARCHITECTURE.md — shutdown flow + memory update mechanism)
  ├── OB-1129 (TROUBLESHOOTING.md — 3 new sections)
  ├── OB-1130 (CONTRIBUTING.md — tsx/Ctrl+C dev tips)
  ├── OB-1131 (CHANGELOG.md — v0.0.5 entries)
  ├── OB-1132 (ROADMAP.md + CLAUDE.md — v0.0.5 milestone)
  └── OB-1133 (final v0.0.5 validation — all docs consistent)
```

---

## Status Legend

|  Status   | Description               |
| :-------: | ------------------------- |
|  ✅ Done  | Completed and verified    |
| 🔄 Active | Currently being worked on |
| ◻ Pending | Not started               |
