# OpenBridge — Task List

> **Pending:** 14 tasks | **In Progress:** 0
> **Last Updated:** 2026-02-25
> **Completed work:** [V0 (Phases 1–5)](archive/v0/TASKS-v0.md) | [V1 (Phases 6–10)](archive/v1/TASKS-v1.md) | [V2 (Phases 11–14)](archive/v2/TASKS-v2.md) | [MVP (Phase 15)](archive/v3/TASKS-v3-mvp.md) | [Self-Governing (Phases 16–21)](archive/v4/TASKS-v4-self-governing.md) | [E2E + Channels (Phases 22–24)](archive/v5/TASKS-v5-e2e-channels.md) | [Smart Orchestration (Phases 25–28)](archive/v6/TASKS-v6-smart-orchestration.md) | [AI Classification (Phase 29)](archive/v7/TASKS-v7-ai-classification.md) | [Production Readiness (Phase 30)](archive/v8/TASKS-v8-production-readiness.md) | [Memory + Scale (Phases 31–38)](archive/v9/TASKS-v9-memory-scale.md)

---

## Phase 40: Memory Integration Hardening

> Fix all gaps between the memory system (SQLite) and the runtime code. The memory modules exist and pass tests, but several code paths still write to JSON files instead of the DB. This phase wires everything end-to-end.

| #   | Task                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | ID     | Priority |  Status   |
| --- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ | :------: | :-------: |
| 270 | **Wire exploration state to SQLite.** In `src/master/exploration-coordinator.ts`, replace all calls to `dotFolder.readExplorationState()` / `writeExplorationState()` with `memory.getSession('exploration')` / `memory.upsertSession()` (or use the `exploration_state` table directly via a new store method). The JSON file `exploration/exploration-state.json` should no longer be written. Update `createExplorationDir()` to stop creating the `exploration/` subdirectory. Keep a read-fallback for the JSON file so existing installs migrate gracefully. Update affected tests in `tests/`. | OB-800 | 🔴 High  | ◻ Pending |
| 271 | **Wire exploration scan results to SQLite.** In `src/master/exploration-coordinator.ts`, replace `dotFolder.readStructureScan()` / `writeStructureScan()`, `readClassification()` / `writeClassification()`, and `readDirectoryDive()` / `writeDirectoryDive()` with writes to the `exploration_state` table or `context_chunks`. Remove the JSON files `exploration/structure-scan.json`, `exploration/classification.json`, and `exploration/dirs/*.json`. Keep read-fallbacks for migration.                                                                                                       | OB-801 | 🔴 High  | ◻ Pending |
| 272 | **Wire exploration.log to the DB.** In `src/master/dotfolder-manager.ts`, replace `logExplorationEntry()` (which appends to `.openbridge/exploration.log`) with a call to a new `memory.logExploration(entry)` method that INSERTs into either `exploration_progress` or a new `exploration_log` column in `exploration_state`. Remove the flat-file write. Add the corresponding method to `src/memory/index.ts`.                                                                                                                                                                                    | OB-802 |  🟡 Med  | ◻ Pending |
| 273 | **Wire agents.json writes to system_config.** In `src/master/dotfolder-manager.ts` method `writeAgents()`, also call `memory.setSystemConfig('agents', registry)` (add this method to MemoryManager if missing). In `readAgents()`, read from DB first, fall back to JSON. Stop writing the JSON file once DB is populated. Same for `src/master/master-manager.ts` `writeAgentsRegistry()`.                                                                                                                                                                                                          | OB-803 |  🟡 Med  | ◻ Pending |
| 274 | **Wire classifications.json writes to system_config.** In `src/master/dotfolder-manager.ts` method `writeClassifications()`, also call `memory.setSystemConfig('classifications', data)`. In `readClassifications()`, read from DB first, fall back to JSON. Stop writing the JSON file once DB is populated. Same for `src/master/master-manager.ts` classification cache persistence at line 1567.                                                                                                                                                                                                  | OB-804 |  🟡 Med  | ◻ Pending |
| 275 | **Complete agent_activity lifecycle.** In `src/master/master-manager.ts`, after a worker completes (both success and failure paths around lines 4060–4145), call `memory.updateActivity(activityId, { status, completedAt, costUsd, resultSummary })`. The `insertActivity()` call at line 3979 returns an ID — store it and pass it to `updateActivity()` when the worker finishes. Verify the `agent_activity` table shows both `running` and `done`/`failed` rows after a message is processed.                                                                                                    | OB-805 | 🔴 High  | ◻ Pending |
| 276 | **Schedule eviction on startup.** In `src/core/bridge.ts` (after MemoryManager init around line 117), call `memory.evictOldData()` once on startup. Then schedule it to run every 24 hours via `setInterval`. Use the default thresholds (conversations: 90 days, tasks: 180 days, chunks: 30 days stale, activity: 24 hours). Log the eviction results.                                                                                                                                                                                                                                              | OB-806 |  🟡 Med  | ◻ Pending |
| 277 | **Wire sub-master registration to SQLite.** In `src/master/sub-master-manager.ts`, when a sub-master is spawned (the `spawnSubMaster()` method), call `memory.registerSubMaster({ workspacePath, parentPath, status, detectedAt })`. When a sub-master completes or fails, call `memory.updateSubMasterStatus()`. Import these from `src/memory/sub-master-store.ts`. Verify the `sub_masters` table has rows after a sub-master is spawned.                                                                                                                                                          | OB-807 |  🟡 Med  | ◻ Pending |
| 278 | **Wire prompt versioning into self-improvement.** In `src/master/master-manager.ts` method `runSelfImprovementCycle()`, when the Master rewrites its own system prompt, call `memory.createPromptVersion('master-system', newContent)` to version it in the `prompts` table. In `seedSystemPrompt()`, check `memory.getActivePrompt('master-system')` first before reading from the file. This enables prompt effectiveness tracking via `recordPromptOutcome()`.                                                                                                                                     | OB-808 |  🟡 Med  | ◻ Pending |
| 279 | **Remove legacy .openbridge subdirectories.** After tasks OB-800 through OB-804 are done, update `createFolder()` in `src/master/dotfolder-manager.ts` to only create `.openbridge/` and `.openbridge/generated/`. Remove creation of `tasks/`, `exploration/`, `exploration/dirs/`, `prompts/`. Update `createExplorationDir()` to be a no-op. Update all affected tests.                                                                                                                                                                                                                            | OB-809 |  🟡 Med  | ◻ Pending |
| 280 | **Add access control CLI write path.** In `src/cli/access.ts` (the `access grant` and `access revoke` subcommands), wire `memory.setAccess()` and `memory.removeAccess()` from `src/memory/access-store.ts`. Verify that `access grant --user X --channel whatsapp --role admin` creates a row in `access_control` and that the auth layer enforces it on the next message.                                                                                                                                                                                                                           | OB-810 |  🟡 Med  | ◻ Pending |
| 281 | **Add integration tests for memory wiring.** Create `tests/integration/memory-wiring.test.ts` that spins up a Bridge with MemoryManager, sends a message through the full pipeline, and verifies: (a) conversations table has user + master rows, (b) tasks table has a task record, (c) learnings table has a learning, (d) context_chunks has exploration data, (e) sessions has the master session, (f) agent_activity has entries with status updates. This test validates the full end-to-end wiring, not individual store modules.                                                              | OB-811 | 🔴 High  | ◻ Pending |
| 282 | **Verify all 1621+ tests pass after wiring changes.** Run `npm run lint && npm run typecheck && npm test && npm run build`. Fix any regressions introduced by OB-800 through OB-811. The WhatsApp reconnect test is known-flaky and can be skipped if it's the only failure.                                                                                                                                                                                                                                                                                                                          | OB-812 | 🔴 High  | ◻ Pending |
| 283 | **Clean up .openbridge on fresh start.** After all wiring is complete, verify that a fresh `npm run dev` (with no `.openbridge/` folder) creates only: `.openbridge/openbridge.db`, `.openbridge/openbridge.db-wal`, `.openbridge/openbridge.db-shm`, `.openbridge/generated/`, and `.openbridge/master-session.json` (if session file is still needed). No `tasks/`, `exploration/`, `prompts/`, `agents.json`, `classifications.json`, or `exploration.log` should be created. Document the final `.openbridge/` structure in `README.md`.                                                          | OB-813 | 🔴 High  | ◻ Pending |

---

## Status Legend

|  Status   | Description               |
| :-------: | ------------------------- |
|  ✅ Done  | Completed and verified    |
| 🔄 Active | Currently being worked on |
| ◻ Pending | Not started               |

---

## Backlog — Unscheduled

| Task                                                                      | ID     | Priority |
| ------------------------------------------------------------------------- | ------ | :------: |
| Migrate master-session.json to sessions table (eliminate last JSON)       | OB-820 |  🟡 Med  |
| Add DB schema versioning (migration table with version number)            | OB-821 |  🟡 Med  |
| Conversation context injection from DB (replace buildConversationContext) | OB-822 |  🟡 Med  |
